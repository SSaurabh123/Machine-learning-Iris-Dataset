#histogram for each flowers Univariate

par(mfrow=c(1,4))
for(i in 1:4) {
  hist(iris[,i], main=names(iris)[i])
}

#-------------------------------------------------------------------------------------------------------------------------------------------------------------
  #3d plot
  
  scatter3d(Petal.Width~Petal.Length+Sepal.Length|Species, data=iris, fit="linear", parallel=FALSE, bg="black", axis.scales=TRUE, grid=TRUE, ellipsoid=FALSE)

#-------------------------------------------------------------------------------------------------------------------------------------------------------------
  #Scatter plot
  
  library(RColorBrewer)
plot(x=iris$Petal.Length,y=iris$Species)

#------------------------------------------------------------------------------------------------------------------------------------------------------------
  #density plot
  
  par(mfrow=c(1,4))
for(i in 1:4) {
  plot(density(iris[,i]), main=names(iris)[i])
}

#-----------------------------------------------------------------------------------------------------------------------------------------------------------
  #box plot
  
  par(mfrow=c(1,4))
for(i in 1:4) {
  boxplot(iris[,i], main=names(iris)[i])
}
#----------------------------------------------------------------------------------------------------------------------------------------------------------
  #correlation plot
  
  
  library(corrplot)
correlations <- cor(iris[,1:4])
corrplot(correlations, method="circle")

#----------------------------------------------------------------------------------------------------------------------------------------------------------
  